import { Text } from "@chakra-ui/react";

function Loading() {
  return (
    <Text m="auto " textAlign={"center"} fontSize={"lg"} fontWeight={"bold"} mt={18}>
      Loading...
    </Text>
  );
}

export default Loading;
